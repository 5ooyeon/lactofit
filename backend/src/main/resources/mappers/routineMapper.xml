<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafit.model.dao.RoutineDao">
	<insert id="insertRoutine" parameterType="Routine">
		<selectKey keyProperty="routine_id" resultType="int"
			order="AFTER">
			SELECT LAST_INSERT_ID()
		</selectKey>
		INSERT INTO Routine (user_id, routine_name, routine_regDate,
		routine_desc, routine_updateDate)
		VALUES (#{routine.user_id}, #{routine.routine_name},
		#{routine.routine_regDate}, #{routine.routine_desc},
		#{routine.routine_updateDate})
	</insert>
</mapper>
