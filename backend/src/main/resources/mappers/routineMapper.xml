<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafit.model.dao.RoutineDao">

    <insert id="insertRoutine" parameterType="com.ssafit.model.dto.Routine">
        INSERT INTO Routine (user_id, routine_name, routine_desc, routine_regDate, routine_updateDate)
        VALUES (#{userId}, #{routineName}, #{routineDesc}, #{routineRegDate}, #{routineUpdateDate})
    </insert>

    <insert id="insertRoutineComponent" parameterType="com.ssafit.model.dto.RoutineComponents">
        INSERT INTO RoutineComponents (exercise_id, routine_id, routineComponents_reps, routineComponents_weight)
        VALUES (#{exerciseId}, #{routineId}, #{routineComponentsReps}, #{routineComponentsWeight})
    </insert>

    <select id="getLastInsertedRoutineId" resultType="int">
        SELECT LAST_INSERT_ID()
    </select>

</mapper>
